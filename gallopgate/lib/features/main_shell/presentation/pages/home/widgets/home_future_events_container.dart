import 'package:flutter/material.dart';
import 'package:gallopgate/features/_common/widgets/container.dart';

class HomeFutureEventsContainer extends StatelessWidget {
  const HomeFutureEventsContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return const GContainer(
      child: Column(
        children: [],
      ),
    );
  }
}
