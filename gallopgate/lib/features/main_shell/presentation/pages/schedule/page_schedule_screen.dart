import 'package:flutter/material.dart';

class PageScheduleScreen extends StatelessWidget {
  const PageScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
