import 'package:flutter/material.dart';

class HorseDetailsView extends StatelessWidget {
  const HorseDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
