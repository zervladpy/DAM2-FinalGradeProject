import 'package:flutter/material.dart';

abstract class GColor {
  // --- surface --- //
  static const surfaceLight = Color(0xFFFAF8F1);
  static const surfaceVariantLigth = Color(0xFFF9F6EE);
  static const surfaceDark = Color(0xFF0F0D13);
  static const surfaceVariantDark = Color(0xFF2B2A29);

  // --- primary --- //
  static const primaryLight = Color(0xFF0F0D13);
  static const primaryDark = Color(0xFF276FBF);
  // --- secondary --- //

  // --- accent --- //

  // --- error --- //

  // --- success --- //

  // --- warning --- //

  // --- neutral --- //
}
