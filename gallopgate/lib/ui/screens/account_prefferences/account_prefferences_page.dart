import 'package:flutter/material.dart';

class AccountPrefferencesPage extends StatelessWidget {
  const AccountPrefferencesPage({super.key});

  static get page => const MaterialPage(child: AccountPrefferencesPage());

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
